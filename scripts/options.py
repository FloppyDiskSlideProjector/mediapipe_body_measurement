import os
class options:
    model_name = "pose_landmarker_heavy.task"
    image_path = os.path.join("image","sample.jpg")
    
    # scale
    # realife life measurement required
    x_scale = 1 # temporary value
    y_scale = 1 # temporary value
    
    